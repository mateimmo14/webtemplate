# WebTemplate
   A template for making a websites in Go, comes with 2 handlers for POST and GET requests
## Structure
* All `.html` files should go in the `resources/html` directory and images should go in the `resources/images` directory
* Any other resources you should create another directory for in resources
